{
  "name": "llwnw-azure-functions",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "deploy": "func azure functionapp publish func-llwnw --build remote"
  },
  "dependencies": {
    "@azure/storage-blob": "^12.10.0",
    "@types/node": "^17.0.35",
    "cross-fetch": "^3.1.5",
    "date-fns": "^2.28.0",
    "date-fns-tz": "^1.3.4",
    "durable-functions": "^1.5.4",
    "playwright-chromium": "^1.22.2"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "typescript": "^4.0.0"
  }
}
